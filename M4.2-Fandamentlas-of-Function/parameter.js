// function parameter
// how its works 
// lets write a function for add 
function add(a, b){ // where a and b is parameter , it can one or multiple ;
    let sum = a + b;
    console.log('sum is : ', sum);
}

// function call
add(4, 3);
add(434, 634);

// one more ! write a function for square 
function square(number) {
    squareNumber = number * number;
    console.log(number, 'square is ', squareNumber);
    
}

// call the function 
square(5);
square(19);

// you do so many nice thing using function 
