// if you need only the first value , its also works like map ; 

numbers = [4, 2, 5, 29, 23, 92, 34];
console.log(numbers.find(num => num >= 10 ));

// it will just return the value which if gater than 10 ; 

