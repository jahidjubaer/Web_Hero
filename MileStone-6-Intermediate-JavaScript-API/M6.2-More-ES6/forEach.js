// if you don't need to return the value then you can use forEach() ; it works like map 

numbers = [3, 4, 6, 2, 9, 0];

numbers.forEach(num => console.log(num)); // look just print the value . it's cant return anything 

const friends = ['rahul', 'rafsan', 'nokib', 'babul'];
friends.forEach((name, index) => {
    console.log(name, index);
})
// its like an cpp void function , it can return anything ; 
