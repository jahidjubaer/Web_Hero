const computer = {
    brand : 'hp',
    price : 630000,
    ram : '8 gb',
    processor : 'AMD Ryzon',
    isGraphicCard : false,
}

// keys 
let keys = Object.keys(computer);
console.log(keys);

// value 
let values = Object.values(computer);
console.log(values);

let str = 'misssdi';
console.log(str[9]);